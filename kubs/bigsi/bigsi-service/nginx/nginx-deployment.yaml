apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: mykrobe-atlas-bigsi-nginx-deployment
  labels:
    app: mykrobe-atlas-bigsi-nginx
    tier: front    
spec:
  selector:
    matchLabels:
      app: mykrobe-atlas-bigsi-nginx
  template:
    metadata:
      labels:
        app: mykrobe-atlas-bigsi-nginx
    spec:
      volumes:       
      - name: configmap-volume
        configMap:
          name: mykrobe-atlas-bigsi-nginx-configmap
      containers:
      - name: mykrobe-atlas-bigsi-nginx
        image: nginx
        command: ["nginx"]
        args: ["-c","/etc/nginx/conf.d/nginx.conf" ,"-g","daemon off;" ]           
        ports:
          - containerPort: 80
        volumeMounts:
        - mountPath: /etc/nginx/conf.d
          name: configmap-volume                  
